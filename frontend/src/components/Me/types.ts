import { INF_User } from "../../features/types";
import { INF_Profile } from "../Modules/Profiles/types";

export interface INF_MeProfile {
    user: INF_User;
}

export interface INF_MeMain {
    user: INF_User;
}